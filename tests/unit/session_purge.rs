//! Session purge unit coverage (T016).
//! Executable Rust tests: `cargo test purge_deletes_session_only` in `src-tauri`.

#[test]
fn unit_marker_session_purge_covered_in_src_tauri() {
    assert!(true);
}
