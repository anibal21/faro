# Splash background image (asset deferred to implement polish)

Place a full-bleed brand image (lighthouse / Faro) at:

- `src/assets/splash-bg.jpg` (or `.png` / `.webp`)

Until then, `SplashView` uses a CSS gradient placeholder slot matching wireframe `01-splash-preparing.svg`.
