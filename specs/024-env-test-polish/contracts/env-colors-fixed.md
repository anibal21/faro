# Contract: Fixed env colors

- `ENV_COLORS[i]` exposes one hex (or light===dark).
- CSS `--env-color-0` … `--env-color-9` identical under light and dark roots.
- `envColorVar(i)` unchanged API.
- Visual: bright / metallic; 10 distinct; readable on light and dark chrome.
